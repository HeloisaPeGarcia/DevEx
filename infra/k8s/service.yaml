apiVersion: v1
kind: Service
metadata:
  name: sample-app
  namespace: preview-env-sample
spec:
  selector:
    app.kubernetes.io/name: sample-app
  ports:
    - name: http
      port: 80
      targetPort: 8080

